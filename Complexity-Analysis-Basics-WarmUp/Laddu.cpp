#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin>>n;
    int act[n], rank[n], sev[n];
    string org[n], co_w[n], t_co[n], b_f[n], c_h[n];;

    for(int i=0;i<n;i++)
    {
        cin>>act[i]>>org[i];

        cin>>co_w[i]>>rank[i];

        cin>>t_co[i];

        cin>>b_f[i]>>sev[i];

        cin>>c_h[i];
    }

    for(int i=0;i<n;i++)
    {
        cin>>act[i]>>org[i];

        cin>>co_w[i]>>rank[i];

        cin>>t_co[i];

        cin>>b_f[i]>>sev[i];

        cin>>c_h[i];
    }

    return 0;
}